# Read by `npm run build`. The Lightsail box behind Caddy. wss, not ws, because an https page is not
# allowed to open a plain socket and the browser blocks it before any traffic leaves.
VITE_SERVER_URL=wss://api.slint.live/ws
