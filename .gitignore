# Dependencies
node_modules/

# Vite build output + its dependency cache
dist/
dist-ssr/
.vite/
*.local

# Go server build output (the compiled binary, named after its folder)
server/server
server/*.exe

# Compiled output of the fixture generator (npm run gen:trajectory)
client/.tools/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local docs (not part of the shipped project)
docs/

# Claude Code related — all of it
.claude/
CLAUDE.md
CLAUDE.local.md
HANDOFF.md

# Firebase CLI deploy cache (hashes of uploaded files, regenerated every deploy)
client/.firebase/
